using System;
using System.Drawing;
using System.Collections.Generic;
using System.Text;

namespace GameExplore
{
    class Program
    {
        static void Main(string[] args)
        {

            // Load the tiles
            //var size = LodePngNative.lodepng_decode32(IntPtr.Zero, IntPtr.Zero, IntPtr.Zero, IntPtr.Zero, 0);
            //Console.WriteLine("size = {0}", size);

            //Bitmap bitmap = new Bitmap("img

            var grass = ResourceManager.LoadImage("grass.png");





        }
    }
}
